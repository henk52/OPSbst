# Theses comments will not be copied over
#  because they are not inside one of the begin-end tags.

%PACKAGELISTBEGIN%
# vagrant related packages.
puppet
augeas
git
yum-utils
wget
perl-YAML-LibYAML
%PACKAGELISTEND%

%POSTBEGIN%
# Getting and executing the puppet module for the vagrant guest.
cd /etc/puppet/modules
git clone http://10.1.3.2:/git/vagrantguest.git vagrantguest
puppet apply /etc/puppet/modules/vagrantguest/manifests/virtual_box_client.pp
%POSTEND%


%POSTBEGIN%
# Install the proxy env module, but do no apply it.
cd /etc/puppet/modules
git clone http://10.1.3.2:/git/proxy_server_env proxy_server_env
%POSTEND%



%POSTBEGIN%
%POSTEND%

